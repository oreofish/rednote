<%# encoding: utf-8 %>

(function() {
<% if @task.valid? %>
  $('#task<%= @task.id %>').removeClass("status1").addClass("status2")
  $('#task<%= @task.id %>').effect('highlight', {}, 3000);
<% else %>
   // something like flashController.doFailure('<p>创建失败</p>');
<% end %>
}());

