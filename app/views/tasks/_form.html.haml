- # encoding: utf-8

.form-actions
  = form_tag tasks_path, :class => :form, :remote => true do
    = text_area :task, :content, :class => 'span7', :rows => 2, :placeholder => "任务描述"
    = hidden_field :project, :name, :value => @current_project
    %br/
    %button.btn.btn-primary.pull-right{ :type=>"submit"} 添加任务
