<%# encoding: utf-8 %>

<div class="form-actions pull-right">
<%= link_to "创建新任务", new_root_tasks_path, :class => 'btn'  %>
</div>

<div class="page-header">
  <h1>Listing tasks</h2>
</div>

<h2>Fluid columns</h2>
<% @tasks.each do |task| %>
  <%= render :partial => 'item', :locals => { :task => task } %>
<% end %>
