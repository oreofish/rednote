<%# encoding: utf-8 %>

<ul class='comment'>
  <% @comments.each do |comment| %>
    <li>
    <%= image_tag(User.find(comment.user_id).avatar) %>
      <%= comment.comment %>
			<% if(comment.user_id==current_user.id) %>
				<%= link_to 'x', comment, :confirm => '你确认删除？', :method => :delete, :class => 'small-button', :remote => true %>
			<%end%>
    </li>
  <% end %>
</ul>

<%= render 'form' %>

