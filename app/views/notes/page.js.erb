(function() {
  var $notes = $('#main-contents ul.list');
  $notes.append( "<%= j render 'notes/list' %> " );

  <% if @notes.size > 0 %>
      setTimeout( function() {
        <% @notes.each do |note| %>
          var obj = $('#note<%= note.id %>');
          removeAlert.bindRemoveAlert(obj);
        <% end %>
      }, 100 );
    <% end %>
});

