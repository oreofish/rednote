<div class="board">
  <%= render :partial => 'userinfo', :locals => { :user => @user } %>

  <h3>同事</h3>
  <% @users.each do |user| %>
    <p><%= link_to user.nickname, user_path(user), :method => :get %></p>
  <% end %>

</div>

